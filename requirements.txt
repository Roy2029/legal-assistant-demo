# RAG 项目依赖

# ── 离线处理 ──────────────────────────────────────────
PyYAML>=6.0
sentence-transformers>=2.2.0
pypdf>=3.17.0
pdfplumber>=0.10.0
Pillow>=10.0.0
python-docx>=1.1.0
python-pptx>=0.6.21
qdrant-client>=1.7.0
numpy>=1.24.0
tqdm>=4.65.0
jieba>=0.42.1

# ── 旧依赖（标记为可选/废弃） ──────────────────────────
# faiss-cpu>=1.7.4    # 已迁移至 Qdrant，保留仅用于兼容旧索引加载
# rank-bm25>=0.2.2    # 已迁移至 Qdrant SparseVector，保留仅用于兼容

# ── 交互分析 ──────────────────────────────────────────
streamlit>=1.28.0
plotly>=5.18.0
pandas>=2.0.0

# ── Web 界面 ──────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
sse-starlette>=2.1.0
jinja2>=3.1.0
python-multipart>=0.0.12
httpx>=0.27.0
python-dotenv>=1.0.0

# ── Token 估算（可选） ──────────────────────────────────
# deepseek-tokenizer>=0.2.0   # DeepSeek 精确 token 计数，未安装时自动降级为字符估算
