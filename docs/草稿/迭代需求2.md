# 迭代需求 2（试用反馈第二轮）

> 状态：2026-08-28 已逐项处理，见各条标注。

1. **bad case 前端反馈按键 + 迭代闭环** —— ✅ 已实现
   - 前端：知识库问答 / 实务助手回答尾部加「有帮助 / 没帮助」；点踩弹出原因标签（检索错/引用错/答非所问/格式差/其他）+ 补充说明。
   - 落库：SQLite `badcase_feedback` 表，含 session_id / trace_id / mode / action / query / answer / reason / note / trace_json / status。
   - 管理：`GET/PUT /api/badcases` + `scripts/export_badcases.py` 导出 JSON/CSV；状态流 new → pending → fixed → closed。
   - 说明：点踩时前端会把当前 trace（或实务助手 steps）一起提交，开发者可回溯。

2. **实务助手前端布局被挤扁** —— ✅ 已修复
   - 左侧业务动作卡片加 `flexShrink:0`；右侧执行区 `minWidth:0`，不再挤压左栏。

3. **知识库问答输出没有 markdown 渲染** —— ✅ 已实现
   - 引入 `react-markdown + remark-gfm`，标题/列表/表格/引用/代码均可渲染。

4. **上传文档存在哪里 / 全流程是否做好** —— ✅ 已做好，前端已标注
   - 文件存 `data/uploads/<doc_id>.<ext>`；流程：解析（docx/md/txt/pdf）→ chunker_v2 切块 → embedding → Qdrant 入库（corpus=user）→ SQLite user_docs 记录。
   - 知识库管理页已显示文件夹、解析状态、chunk 数，并提示存储位置。

5. **应用内查看切分情况、手动调整分块** —— ⚠️ 查看已实现，手动调整未做
   - 已实现：知识库管理页每篇文档「查看分块」，展示每个 chunk 的文本、条号、tokens、文件夹等。
   - 未实现：合并 / 拆分 / 编辑。下一步按「拆分 → 编辑 → 合并」顺序补。

6. **文件夹管理 + 文件夹元数据过滤 + 问答中选择激活** —— ✅ 已实现
   - 知识库管理页：新建文件夹、选择上传目标文件夹、按文件夹筛选文档。
   - Qdrant 元数据：用户 chunk 写入 `metadata.folder` 与 `metadata.kb_id`。
   - 知识库问答页：顶部新增知识库选择器（全部 / 公共法律库 / 各用户文件夹），通过 `metadata.folder` 过滤检索。

7. **trace 长文档名遮挡「查看原文」按钮** —— ✅ 已修复
   - chunk 卡片标题行 `flexWrap` + 标签 `wordBreak:break-all`，按钮 `flexShrink:0`，长文档名自动换行。
